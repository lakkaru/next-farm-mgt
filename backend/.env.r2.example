# Cloudflare R2 Configuration
# Copy this to your .env file and fill in your actual values

# R2 Endpoint URL (replace YOUR_ACCOUNT_ID with your actual Cloudflare account ID)
R2_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com

# R2 Access Credentials (from R2 API tokens)
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key

# R2 Bucket Name
R2_BUCKET_NAME=farm-management-images

# Your Cloudflare Account ID
R2_ACCOUNT_ID=your_account_id

# Optional: Custom domain for R2 public access (if you've set up a custom domain)
# R2_PUBLIC_URL=https://images.yourdomain.com

# Instructions:
# 1. Go to Cloudflare Dashboard → R2 Object Storage
# 2. Create a bucket (e.g., 'farm-management-images')
# 3. Go to Manage R2 API tokens → Create API token
# 4. Grant permissions: Object Read & Write for your bucket
# 5. Copy the Access Key ID and Secret Access Key
# 6. Replace the values above with your actual credentials
# 7. Add these to your .env file in the backend folder
